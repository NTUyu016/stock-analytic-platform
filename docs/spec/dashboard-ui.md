# 儀表板介面 v1

決策來源：[issue #11](https://github.com/NTUyu016/stock-analytic-platform/issues/11)。名詞定義見 [`CONTEXT.md`](../../CONTEXT.md)，技術棧見 [`tech-stack.md`](./tech-stack.md)。

原型（丟棄式，不進 master）：分支 [`prototype/dashboard-layouts`](https://github.com/NTUyu016/stock-analytic-platform/tree/prototype/dashboard-layouts)。經五輪回饋後選定**變體 D「工作台」**。

> v1 定案。使用者註明「UI 還可以，未來再調整」—— 本文記錄的是 v1 的起點，不是終局。

---

## 0. 站台頁面與路由（[#18](https://github.com/NTUyu016/stock-analytic-platform/issues/18) 於 2026-08-09 補）

> 2026-08-09 的冷讀驗收發現：**整份規格只定義了「儀表板」一頁**，但另外四份文件各自要求了一些畫面（`performance.md` §7 的圖、`analysis-dimensions.md` 的個股分析頁、`alerts.md` §9 的規則面板、`transaction-input.md` §9 的資料新鮮度指示器），**而沒有任何一處說它們放在哪一頁、路由長什麼樣**。實作者拿到的是一堆元件、沒有站台。

| 路由 | 頁面 | 內容 | 規格出處 |
|---|---|---|---|
| `/` | **儀表板** | 側欄 + 單頁三層（§1）：總資產與四個報酬指標、資產走勢圖、持股表 | 本文 §1／§5／§7 |
| `/instruments/:id` | **個股分析頁** | 六維分數卡列表、Coverage、評等 | [`analysis-dimensions.md`](./analysis-dimensions.md) |
| `/transactions` | **交易明細** | **依市場拆表**（§6） | 本文 §6 |
| `/transactions/import` | **匯入** | CSV 上傳、衝突與中斷的處理畫面、匯入摘要 | [`transaction-input.md`](./transaction-input.md) §1／§4／§6 |
| `/performance` | **績效** | `performance.md` §7 的圖、大盤比較、XIRR 與其 SUPPRESS 說明 | [`performance.md`](./performance.md) §7 |
| `/alerts` | **規則面板** | 平鋪不摺疊；顯示 `alert_state.suspended_reason` | [`alerts.md`](./alerts.md) §9 |
| `/login` | **登入** | ⚠️ **只能是登入頁**——不得出現任何即時報價或其衍生數值 | [`auth.md`](./auth.md)、[`tech-stack.md`](./tech-stack.md) §8 |

**跨頁面、常駐於側欄的三個東西**（不是獨立路由）：

| 元件 | 顯示條件 | 出處 |
|---|---|---|
| 連線狀態指示器 | 永遠 | 本文 §3 |
| **待確認項清單** | **只在有未確認項時出現** | 本文 §9A |
| 資料新鮮度指示器 | 盤後排程落後時 | [`transaction-input.md`](./transaction-input.md) §9 |

**v1 刻意不做的頁面**：設定頁（評等切點等設定值以 `.env` 或設定檔提供即可，`deployment.md` §6）、使用者管理（單人，走 CLI）、美股與加密貨幣分析頁（`analysis-dimensions.md` 已定 v1 不做）。

> ⚠️ **`/instruments/:id` 與 `/performance` 的版面沒有原型、也沒有版面規格。** #11 的原型只做了儀表板。本文 §2（顏色）、§3（連線狀態）、§4（tick）、§8（字型）、§9（實作規則）是**全站適用**的硬約束，但那兩頁的**版面配置**是實作階段的自由度——這是誠實記錄，不是遺漏。

---

## 1. 版面：側欄 + 單頁三層

```
┌────────┬──────────────────────────────────────────┐
│        │  總資產（1.75fr）        │  警示（1fr）   │
│  側欄  ├──────────────────────────────────────────┤
│ 208px  │  持股（全寬）                             │
│        ├──────────────────────────────────────────┤
│        │  最近交易（1.75fr）      │ 資產配置（1fr）│
└────────┴──────────────────────────────────────────┘
```

**為什麼是這個形狀**（依序推翻的三個候選）：

| 候選 | 為什麼不是它 |
|---|---|
| 頂部分頁 + 大 KPI 橫幅 | 分頁把警示與交易藏起來，開頁看不到今天發生什麼事 |
| 側欄 + 持股列表流 | 總資產壓成一行太弱，歷史走勢沒有位置 |
| 兩欄網格、五個小面板 | 使用者評語：**「太分散又資訊不清楚」**。面板太多太小，每個都放不下完整資訊 |

**關鍵修正**：不是「把面板搬來搬去」，而是**把面板改少改大**。從五個小面板收斂成四個，其中兩個給全寬。

**對稱靠三件事達成**（都是可驗證的規則，不是感覺）：
1. 第 1 層與第 3 層用**同一組欄寬比例 1.75 : 1**，兩層的欄縫上下對齊。
2. 全域變數 `--pad`（卡片水平內距）與 `--hd-h`（標題列高度），所有卡片吃同一組值。
3. 警示與最近交易列使用**同一種列結構**，並排時列高與對齊點才會咬合。

**手機**：側欄收成頂部橫向捲軸，所有分欄堆成單欄。使用者對此版面在手機上的評價高於在桌面上 —— 堆疊式面板在窄螢幕是優勢。

---

## 2. 顏色：只保留給價格方向

**紅漲綠跌，統一台股慣例，不做切換。** 美股標的（NVDA / VOO）也照台股顯示。

由此推出一條**硬性規則**：

> **介面其他任何地方都不得出現紅色或綠色** —— 按鈕、選中的分頁、側欄目前項、標籤都不行。

否則眼睛會把它讀成漲跌訊號。所有互動元素一律用**墨色**（`--ink`），沒有彩色 accent。這是被領域逼出來的設計立場，不是美感偏好。

配色詳見原型的 token 定義。類別色（資產配置）在淺色模式對比低於 3:1，因此**每一段都必須有可見的直接標籤**（不可只靠 hover）。

---

## 3. 連線狀態：三態，不是兩態

| 狀態 | 文案 | 語氣 | 視覺 |
|---|---|---|---|
| 即時 | `即時連線 09:00-13:30` | 正常 | 實心圓點 · 中性色 |
| **非交易時段** | `非交易時段 13:30-09:00` | **正常，不是錯誤** | 空心圓點 · **中性色** |
| **盤中斷線** | `盤中斷線 最後報價 11:42` | **異常** | 旋轉方塊 · **琥珀色** |

**為什麼非交易時段不能用警告色**：每天下午三點打開網站被一片琥珀色迎接，久了就會學會忽略它 —— 等到真的盤中斷線時也不會看了。警告色的價值來自稀有。

**兩種降級都必須同時做兩件事**，只做一件不夠：
1. 顯示橫幅說明現在看到的是什麼
2. **所有報價數字降透明度**（否則會盯著舊價格好幾分鐘沒發現）

**連線指示器絕不可使用紅綠燈** —— 綠燈=正常在此系統中會與「跌」衝突。改用**形狀 + 文字 + 琥珀色**。

文案規則：**無標點、單句、標註時段**。

台股集中市場交易時段為 **09:00–13:30**（盤後零股至 14:30）。

> ### ⚠️ 2026-08-09 由 [#18](https://github.com/NTUyu016/stock-analytic-platform/issues/18) 補：13:30–13:35 這五分鐘
>
> 上表的文案寫死了 `09:00-13:30`／`13:30-09:00`，但 [`realtime-quotes.md`](./realtime-quotes.md) §8 定 `quote-worker` 的開機時段是 **09:00–13:35**（多留五分鐘收尾盤與試撮後的最後成交）。**這五分鐘落在兩個文案之間，沒有答案。**
>
> **決定：13:30–13:35 顯示「即時」態。** 理由是這一格要回答的問題是「**現在畫面上的數字還會不會跳**」，而那五分鐘 worker 還連著、收盤前的最後成交確實還會進來。若顯示「非交易時段」，使用者會以為數字凍結了，然後看著它跳。
>
> **連帶的硬性規則**：三態的判定**不得寫死時間字面量**，一律讀 `realtime-quotes.md` §8 那份時段設定。文案的時間字串也由同一份設定產生——**兩處各自寫死，就是這個 bug 的成因**。

---

## 4. 即時跳動：細直條，背景永不變

價格更新時，**列的左緣閃一條 3px 直條，800ms 淡出**。背景色**永遠不動**。

被否決的做法：整列或整格的背景色殘留 —— 盯久了會累，且在密集表格中會製造整片閃爍。

**這給 [#13](https://github.com/NTUyu016/stock-analytic-platform/issues/13) 一個具體上限**：動畫 800ms，**推播節流頻率快過這個數字沒有意義** —— 眼睛還沒看完上一次閃動，下一次已經蓋上來。

實作注意：在表格中，直條用 **`td` 的 `border-left`** 實作並動畫 `border-left-color`，**不可**把儲存格改成 `display:flex` 塞子元素（會破壞表格佈局）。

---

## 5. 持股表欄位

| 標的 | 當日走勢 | 股數 | 成本均價 | 現價 | 漲跌 | 市值 | 未實現損益 | 報酬率 |
|---|---|---|---|---|---|---|---|---|

- **成本均價與股數**用次要墨色，與即時數字分層
- **報酬率**加粗，是視線最右的落點
- **當日走勢**併為一欄（迷你走勢圖）
- **不放「佔比」** —— 資產配置面板已在講同一件事
- 數字欄一律 `tabular-nums` 並右對齊

> **[#16](https://github.com/NTUyu016/stock-analytic-platform/issues/16) 修訂**：本表的「報酬率」是**簡單報酬率且不含股利**，因此與券商 App 的數字對得上。**必須在欄位標題或說明處標示「不含股利」**。
> 含股利的版本（「含股利總報酬」與「已領股利」兩個**絕對金額**）放在**展開列**，不放在列上 —— 同一張表出現兩個都叫「報酬率」的欄位，是製造誤讀的最短路徑。詳見 [`performance.md`](./performance.md) §4.1。

---

## 6. 交易明細：依市場拆表

分成 **台股 · 新臺幣** 與 **美股 · 美元** 兩段，**幣別在分段小標宣告一次，列上完全不標記單位**。

| 日期 | 標的 | 別 | 股數 | 成交價 | 費用 | 淨額 |
|---|---|---|---|---|---|---|

**拆表的理由不是版面，是欄位語意**：

| | 台股 | 美股（複委託） |
|---|---|---|
| 手續費 | 0.1425% × 券商折讓，最低 20 元 | **有最低收費，比例公式失效** |
| 交易稅 | 賣出證交稅 0.3%／ETF 0.1% | **沒有** |
| 股利 | 併入所得，有補充保費門檻 | **就源扣繳 30%** |

擠在同一個「費用」欄裡，那些數字是**三種不同的東西**，既不能比較也不能加總。

**顯示合併、底層分開**：`transaction` 表維持 `fee` 與 `tax` 兩個獨立欄位——兩者的費率規則與落日條款各自演進（見 [#6](../research/tw-trading-costs-taxes.md)），合併儲存會把它們綁死。

**外幣列**維持原幣別顯示（美股股價本就是美元計價），台幣等值放在 hover。若要換算，**一律採成交當日匯率，不得用今日匯率** —— 否則已經發生的歷史紀錄會每天顯示不同數字（[#9](https://github.com/NTUyu016/stock-analytic-platform/issues/9) 已定調）。

> **本項驗證了 #9 的一個決定**：因為金額存原幣別、只在顯示層換算，這個拆表需求**完全不需要動資料庫**。若當初決定入庫即換算成台幣，此需求會逼著改綱要。

---

## 7. 資產走勢：可拖曳選取區間

按住拖曳選取任意區間，顯示該段的 **絕對變化 + 百分比 + 方向 + 日期區間 + 交易日數**。

- 區間內用方向色描邊並填色，**區間外壓淡至 32%**
- 兩端虛線界線 + 端點圓點
- 單擊或 `Esc` 取消；切換期間（1M/3M/6M/1Y/**全部**）亦清除
- 未選取時為十字準星 + 單日 tooltip；選取後準星讓位，避免兩套游標打架

~~⚠️ **這個互動對後端有實質需求**：查詢必須夠快以支撐 60fps 的拖曳，這使得「快照 vs 每次重算」的成本差距遠大於原先估計。~~

> **[#16](https://github.com/NTUyu016/stock-analytic-platform/issues/16) 撤回上述判斷。** 拖曳操作的是**瀏覽器裡已經載入的那條序列**，完全不打後端 —— 後端只在切換期間時回一次完整序列（1 年約 244 個點，個位數 KB）。**60fps 是前端的事，與資料庫無關。**
> 這條顧慮曾是「該不該建 `portfolio_snapshot` 快取」最主要的推力，撤回後 #16 定案為**每次重算、不建快取**。期間選項另補上「全部」（曲線起點為最早一筆交易日）。詳見 [`performance.md`](./performance.md) §1.3。

---

## 8. 字型

**Noto Sans TC（介面與中文） + Tahoma（數字）**

⚠️ **Noto Sans TC 不是 Windows 內建字型。** 正式站**不可假設訪客已安裝** —— 必須自行 host 字型檔並做**子集化**（完整中文字型 5–15 MB，子集化後可壓至數百 KB）。中文字型子集化是獨立的工程課題，列入實作待辦。

**字型策略必須包含 fallback 鏈的明確驗證。** 靜默 fallback 是前端最容易漏掉的問題：指定一個未安裝的字型，畫面照樣渲染、零錯誤，只是長得跟預期不同。原型內建了以 canvas 量測寬度的偵測機制，實作時應保留等效的驗證手段。

---

## 9. 原型過程中撞出的實作規則

這三條都是實際踩到的 bug，寫成規則避免重演：

**（a）同一標籤不得重複屬性。**
原型曾在同一個 `div` 上寫了兩個 `style`，HTML 解析器只取第一個、靜默丟棄第二個 —— `overflow-x:auto` 從未生效，表格撐破容器。lint 可攔截。

**（b）表格儲存格不得更改 `display`。**
為了塞 tick 直條而把 `td` 設成 `display:flex`，會使該儲存格脫離表格佈局、欄寬對不齊。需要在儲存格邊緣加視覺元素時用 `border`。

**（c）「建立 DOM」與「綁定事件」必須分離；重繪路徑上不得呼叫 `addEventListener`。**
原型的曲線元件把繪圖與綁定寫在同一個函式，而重繪只替換 `innerHTML`（動不到掛在外層容器上的監聽器）。拖曳時每次 `mousemove` 觸發重繪、每次重繪疊加一組監聽器 —— **監聽器數量指數成長，第二次拖曳即當機**。

> React 的宣告式渲染天然避開 (c)，但 `useEffect` 內手動綁定 listener 而依賴陣列寫錯會完全重現同一個 bug。這與 [`tech-stack.md`](./tech-stack.md) §3 提到的「持股表必須做成每列各自訂閱」是同一類風險：**React 只是讓你不容易寫錯，不是不可能寫錯。**

高頻互動（拖曳、即時 tick）的重繪應以 `requestAnimationFrame` 合併，每影格至多一次。

---

## 9A. 待確認項區塊（[#20](https://github.com/NTUyu016/stock-analytic-platform/issues/20) 新增）

原型只定義了警示規則面板，**沒有 `pending_action`（待確認項）的呈現方式**。#20 補上最小規格：

| 項目 | 規則 |
|---|---|
| **位置** | 側欄，**只在有未確認項時出現** |
| **為什麼不常駐** | 常駐一個永遠是 0 的計數器，等於訓練使用者不看它——與 §3「非交易時段用警告色會讓使用者學會忽略它」是同一條原則 |
| **顏色** | **琥珀**（與「盤中斷線」同一個「異常但非價格」的色階）。依 §2 的硬性規則，紅綠只保留給價格方向，**此處不得使用** |
| **排序** | **會改變股數的排前面**（分割、股票股利、減資），現金股利在後 |
| **每項必須顯示** | 標的、生效日、事件類型、系統預填值，以及 **「這個項目目前讓什麼功能暫停了」** |

最後一欄是關鍵：[`alerts.md`](./alerts.md) 已定「未確認的改股數事件會讓該標的的警示規則暫停評估」。**那個後果必須出現在使用者看得到的地方**，否則他日後只會納悶為什麼追蹤停損沒動——而那正是本專案反覆點名的靜默失效。

---

## 10. 留給其他票

| 議題 | 票 |
|---|---|
| ~~推播節流頻率~~ → **已定為 800ms 全域節拍批次送**（見 [`realtime-quotes.md`](./realtime-quotes.md) §6）。§4 的 800ms 動畫直接成為節流節拍，整張持股表每 800ms 同步刷一次，對齊 §9 要求的單一 `requestAnimationFrame` 合併點 | [#13](https://github.com/NTUyu016/stock-analytic-platform/issues/13) |
| 個股分析頁的呈現形式與五級建議 | [#14](https://github.com/NTUyu016/stock-analytic-platform/issues/14) |
| 警示卡片要顯示哪些規則類型 | [#15](https://github.com/NTUyu016/stock-analytic-platform/issues/15) |
| ~~每日總資產如何取得（快照 vs 重算）、報酬率演算法~~ → **已定為每次重算、不建快取**；報酬率分四個指標各有其位（見 [`performance.md`](./performance.md)）。連帶修訂本文 §5 與 §7 | [#16](https://github.com/NTUyu016/stock-analytic-platform/issues/16) |
